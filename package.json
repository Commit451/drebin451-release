{
  "name": "drebin451-release",
  "version": "1.0.0",
  "description": "GitHub Action for uploading Android APK releases to Drebin451.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/main.ts --out dist --source-map --license licenses.txt",
    "check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.8.3"
  }
}
